-- Lists all record of the table second_table of database hbtn_0c_0. 
-- Displays only scores and names ordered by scores in descending order

SELECT score, name
FROM second_table 
ORDER BY score DESC
